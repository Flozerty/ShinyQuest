{% extends 'base.html.twig' %}

{% block title %}Password Reset Email Sent{% endblock %}

{% block body %}
    <p>
        Si un compte existe avec l'adresse email renseignée, un lien de réinitialisation de mot de passe lui a été envoyé.
        {# This link will expire in {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}. #}
    </p>
    <p>Si vous ne trouvez pas votre email, regardez dans votre boîte spam ou <a href="{{ path('app_forgot_password_request') }}">réessayez</a>.</p>
{% endblock %}